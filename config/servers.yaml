# Clothes Recommend System — MCP server registry
#
# local  -> STDIO transport (orchestrator-managed process lifecycle)
# remote -> Streamable HTTP transport (independently deployed service)

servers:
  clothes_recommend_local:
    type: local
    transport: stdio
    command: python
    args:
      - servers/local_stdio/server.py
    tools:
      - get_location_weather
      - recommend_clothes
      - recommend_clothes_for_location
    description: Local Clothes Recommend MCP runtime (STDIO)

  clothes_recommend_remote:
    type: remote
    transport: streamable-http
    url: http://localhost:8000/mcp
    tools:
      - get_location_weather
      - recommend_clothes
      - recommend_clothes_for_location
    description: Remote Clothes Recommend MCP runtime (Streamable HTTP)
